% Limpar variáveis

% Fazer o setup (chamada do código) dos dois modelos (real e fic) da robo

% Loopar N vezes com posições absolutas para o pé distintas, sempre R
% (postura) = eye(3)

    % Realizar chamada da cinemática inversa numérica (pode ser necessário
    % edição) no modelo fictício com junta UY do pé

    % Capturar as posições ("q") das juntas existentes 

    % Enviar essas posições para as juntas correpondetes no modelo real
    
    % Computar o "desenho" atual da robô com a ForwardKinematics no modelo
    % real
    
    % Desenhar a robô (DrawRobot)

    % Printar (existem algumas funções que fazem isso - pesquisar) e salvar em
    % algum diretório específico
    
    % Fechar o "desenho" (gráfico) para o pc não explodir na 20º iteração

% Fim do loop